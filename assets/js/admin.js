// Admin enhancements (placeholder for future features).
(function($){
	$(function(){
		// Confirmação extra em remoção
		$('a[onclick*="Confirmar exclusão"]').css('cursor','pointer');
	});
})(jQuery);
